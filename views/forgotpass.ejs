<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Password Reset</title>
    <style>
        /* Basic reset and full-screen background */
        body, html {
            margin: 0;
            padding: 0;
            height: 100%;
            width: 100%;
            overflow: hidden;
            display: flex;
            justify-content: center;
            align-items: center;
            background: url("/img/Background_3.png") center/cover no-repeat fixed;
            font-family: "Inter", Helvetica, Arial, sans-serif;
            font-size: 1rem;
        }

        .forgetPass-email-page {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100%;
            width: 100%;
            overflow-x: hidden;
            overflow-y: auto;
            padding: 20px;
            box-sizing: border-box;
        }

        .scroll-wrapper {
            display: flex;
            justify-content: center;
            align-items: center;
            width: 100%;
            padding: 20px;
            box-sizing: border-box;
        }

        .frame {
            position: relative;
            -webkit-backdrop-filter: blur(2.5vw) brightness(100%);
            backdrop-filter: blur(2.5vw) brightness(100%);
            background-color: rgba(255, 255, 255, 0.1);
            border-radius: 2vw;
            width: 90%;
            max-width: 30rem;
            padding: 2rem;
            display: flex;
            flex-direction: column;
            align-items: center;
            box-sizing: border-box;
        }

        .frame::before {
            content: '';
            position: absolute;
            top: -0.6vw;
            left: -0.6vw;
            right: -0.6vw;
            bottom: -0.6vw;
            border: 0.3vw solid rgba(255, 255, 255, 0.79);
            border-radius: 2.2vw;
            z-index: -1;
        }

        .text-wrapper {
            color: #ffffff;
            font-size: 2rem;
            font-weight: 600;
            margin-bottom: 1.5rem;
            margin-top: 0.75rem;
            text-align: center;
        }

        .input-wrapper {
            width: 100%;
            position: relative;
            margin-bottom: 1rem;
            display: flex;
            flex-direction: column;
            align-items: center; /* Center items horizontally */
            text-align: center; /* Center content inside */
        }

        .text-wrapper-2 {
            color: #ffffff;
            font-size: 1.25rem;
            font-weight: 600;
            margin-bottom: 0.75rem;
            text-align: center;
            width: calc(100% - 20px);
        }

        .email-input {
            font-size: 1.25rem;
            padding: 0.5rem;
            border-radius: 1.25rem;
            border: 1px solid #000;
            background: #F8EDED;
            width: 100%;
            max-width: 36rem;
            box-sizing: border-box;
        }

        .submit-button {
            width: 60%;
            max-width: 14rem;
            padding: 0.75rem;
            border-radius: 2rem;
            border: 1px solid #000;
            background: #B090FC;
            color: #FFF;
            font-size: 1.25rem;
            cursor: pointer;
            text-align: center;
            margin-top: 1.75rem;
            display: inline-block; /* Ensure button is treated as inline-block */
            transition: background 0.3s;
        }

        .submit-button:hover {
            background: #9e7efc;
        }

        /* Media query for landscape mode */
        @media (orientation: landscape) {
            .forgetPass-email-page {
                padding: 10px;
            }

            .frame {
                width: 80%;
                padding: 1.5rem;
            }
        }
    </style>
</head>
<body>
    <div class="forgetPass-email-page">
        <div class="scroll-wrapper">
            <div class="frame">
                <div class="text-wrapper">Password Reset</div>
                <div class="input-wrapper">
                    <div class="text-wrapper-2">Enter your email address</div>
                    <form action="/user/forgotpass" method="post">
                        <input
                            type="email"
                            name="email"
                            class="email-input"
                            placeholder="username@gmail.com"
                            required
                        />
                        <button type="submit" class="submit-button">Submit</button>
                    </form>
                </div>
            </div>
        </div>
    </div>
</body>
</html>