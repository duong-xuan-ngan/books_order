<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Password Reset</title>
    <style>
        body, html {
            margin: 0;
            padding: 0;
            height: 100%;
            width: 100%;
            overflow: hidden;
            display: flex;
            justify-content: center;
            align-items: center;
            background: url("/img/Background_3.png") center/cover no-repeat fixed;
        }

        .reset-page {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100%;
            width: 100%;
            overflow-x: hidden; /* Disable horizontal scrolling */
            overflow-y: auto; /* Allow vertical scrolling */
        }

        .frame {
            position: relative;
            -webkit-backdrop-filter: blur(25px) brightness(100%);
            backdrop-filter: blur(25px) brightness(100%);
            background-color: #ffffff1a;
            border-radius: 20px;
            width: 90%; /* Responsive width */
            max-width: 500px; /* Maximum width for larger screens */
            padding: 20px;
            display: flex;
            flex-direction: column;
            align-items: center;
            z-index: 1;
            box-sizing: border-box; /* Ensure padding is included in width */
        }

        .frame::before {
            content: '';
            position: absolute;
            top: -6px;
            left: -6px;
            right: -6px;
            bottom: -6px;
            border: 3px solid rgba(255, 255, 255, 0.79);
            border-radius: 22px;
            z-index: -1;
        }

        .text-wrapper {
            color: #ffffff;
            font-family: "Inter", Helvetica, Arial, sans-serif;
            font-size: 40px;
            font-weight: 600;
            margin-bottom: 0.5px;
        }

        .an-email-with-a {
            color: #ffffff;
            font-family: "Inter", Helvetica, Arial, sans-serif;
            font-size: 20px;
            font-weight: 600;
            text-align: center;
            margin-bottom: 20px;
        }

        .span {
            font-weight: 300;
        }

        /* Media query for landscape mode */
        @media (orientation: landscape) {
            .reset-page {
                overflow-y: auto; /* Ensure vertical scrolling */
            }
            .frame {
                width: 90%; /* Keep the frame width responsive */
                max-width: 500px; /* Ensure max-width remains for landscape */
                margin: auto; /* Center the frame */
            }
        }
    </style>
</head>
<body>
    <div class="reset-page">
        <div class="frame">
            <div class="text-wrapper">Password Reset</div>
            <p class="an-email-with-a">
                <span class="span">An email with a password reset link has been sent to your email:</span><br />
                <span class="text-wrapper-2"><%= email %></span><br />
                <span class="span">Check your email and click on the link to proceed!</span>
            </p>
            <a href="/user/reset/<%= token %>" class="button">Reset Password</a> 
        </div>
    </div>
</body>
</html>